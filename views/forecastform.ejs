<%- include('components/header') -%>

<div class="container">
    <div class="row justify-content-center mt-5">
        <% if (mode === 'create') { %>
            <%- include('partials/createForecast', { user, btn }) -%>
        <% } %>
        <% if (mode === 'edit') { %>
            <%- include('partials/editForecast', { user, forecast }) -%>    
        <% } %>
        <% if (mode === 'convert') { %>
            <%- include('partials/convertForecast', { user, forecast }) -%>    
        <% } %>
    </div>
</div>
<%- include('components/footer') -%>