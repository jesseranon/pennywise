<div class="modal" tabindex="-1" aria-labelledby="deleteModalTitle" aria-hidden="true" role="dialog" id="deleteModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalTitle">Confirm delete</h5>
                <button type="button" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete the <span id="deleteModalTextDocType"></span> on <span id="deleteModalTextDate"></span> for the category <span id="deleteModalTextCategory"></span> in the amount of <span id="deleteModalTextAmount"></span>?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="deleteModalCancelBtn">Cancel</button>
                <a class="btn btn-primary" id="deleteModalDeleteBtn">Delete</a>
            </div>
        </div>
    </div>
</div>